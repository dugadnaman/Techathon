'use client';

import { memo } from 'react';
import type { AlertLayerProps } from './types';

function AlertLayerComponent(_props: AlertLayerProps) {
  return null;
}

export const AlertLayer = memo(AlertLayerComponent);
