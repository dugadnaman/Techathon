'use client';

import { memo } from 'react';
import type { RouteLayerProps } from './types';

function RouteLayerComponent(_props: RouteLayerProps) {
  return null;
}

export const RouteLayer = memo(RouteLayerComponent);
