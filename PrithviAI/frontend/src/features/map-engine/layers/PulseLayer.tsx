'use client';

import { memo } from 'react';
import type { PulseLayerProps } from './types';

function PulseLayerComponent(_props: PulseLayerProps) {
  return null;
}

export const PulseLayer = memo(PulseLayerComponent);
