'use client';

import { memo } from 'react';
import type { HeatmapLayerProps } from './types';

function HeatmapLayerComponent(_props: HeatmapLayerProps) {
  return null;
}

export const HeatmapLayer = memo(HeatmapLayerComponent);
